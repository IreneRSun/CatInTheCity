class TrendReport {
    // constructs a report on current world trends
    // includes the top 5 google searches (scraped from Google Trends)
    constructor() {

    }

    // fetches the weather info for the user's location
    fetch_trends() {
        const fetch = require("node-fetch");
        const cheerio = require("cheerio");
        const url = "";
    }

}